import React from 'react';
import { Text, StyleSheet } from 'react-native';

const GoogleScreen = ({ navigation }) => {
  return <Text>Google Screen</Text>;
};

const styles = StyleSheet.create({});

export default GoogleScreen;
